package lk.HostelManagementSystem.bo.custom;

public enum BOType {
    public enum BOType {
        USER,
        ROOM,
        STUDENT,
        RESERVATION,
        ADDUSER,
        SETTING,
        LOGIN,
        PAYEMENT;

        private BOType() {
        }
    }

}
